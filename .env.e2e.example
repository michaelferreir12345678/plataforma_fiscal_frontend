# Ambiente isolado de teste. Copie para .env.e2e (arquivo ignorado pelo Git).
# Nunca reutilize credenciais de produção.
E2E_API_BASE_URL=http://localhost:8000
E2E_EMAIL=e2e-admin@example.invalid
E2E_PASSWORD=defina-uma-senha-local

# Opcional: use uma aplicação de homologação já publicada e não suba o Vite local.
# E2E_BASE_URL=https://frontend.homologacao.example.invalid
